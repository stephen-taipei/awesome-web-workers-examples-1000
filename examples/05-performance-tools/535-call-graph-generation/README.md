# 呼叫圖生成 (Call Graph Generation)

這個範例展示了如何追蹤並視覺化函數之間的呼叫關係（Caller-Callee 關係）。

## 原理

透過維護一個全域的呼叫堆疊 (Call Stack) 和樹狀結構，當一個函數被呼叫時：
1.  查看堆疊頂端的函數（即 Caller）。
2.  在 Caller 的子節點列表中尋找或創建當前函數 (Callee) 的節點。
3.  將 Callee 推入堆疊，執行函數。
4.  函數執行完畢後，從堆疊彈出。

最終生成的樹狀結構即為 **Call Tree**，可以直觀地展示程式的執行流。
