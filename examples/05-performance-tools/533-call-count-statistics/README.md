# 呼叫次數統計 (Call Count Statistics)

這個範例展示了如何統計函數被呼叫的次數。這對於識別「熱點代碼」(Hot Paths) 非常有用，即那些執行頻率最高的函數。

## 原理

通過一個簡單的 Wrapper 函數，在每次目標函數被呼叫時遞增一個計數器。

## 應用

優化程式碼時，應該優先關注被呼叫次數最多的函數，因為即使微小的優化也能在多次執行中累積出顯著的效能提升。
