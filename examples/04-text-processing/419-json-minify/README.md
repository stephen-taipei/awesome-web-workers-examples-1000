# JSON 壓縮 (JSON Minify)

本示例展示了如何使用 Web Workers 對 JSON 數據進行最小化壓縮（移除空白與換行）。

## 功能特點

1.  **JSON 最小化**:
    - 移除所有不必要的空白字符、縮排和換行符。
    - 顯著減少數據體積，適合網絡傳輸前處理。

2.  **壓縮率計算**:
    - 自動計算並顯示壓縮後的體積節省百分比。

3.  **效能**:
    - 將解析和序列化任務移至 Worker 線程，避免主線程卡頓。
